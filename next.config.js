/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["qurucvyalhuslixwlwiz.supabase.co"],
  },
};

module.exports = nextConfig;
